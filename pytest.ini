[pytest]
# Pins rootdir to the repository root. Without an ini file here, running pytest
# from inside tests/ makes that directory the rootdir, which puts the top-level
# conftest.py outside confcutdir: it never loads, the repo root never reaches
# sys.path, and every plain "import fp_analysis_gui" fails to collect.
testpaths = tests
